<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Dashboard - Medicine Tracker</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
</head>
<body class="bg-light">

<nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
    <div class="container">
        <a class="navbar-brand" href="{{ url_for('dashboard') }}">Medicine Tracker</a>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link" href="#">Hello, {{ username }}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url_for('alternative_medicines') }}">WHO Alternatives</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url_for('reminder') }}">Reminders</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url_for('add_medicine') }}">Add Medicine</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url_for('logout') }}">Logout</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<div class="container mt-5 animate-fade-in">
    <h2 class="mb-3 text-center">Welcome, {{ username }}!</h2>
    <p class="text-muted text-center">Manage your medicines and reminders below.</p>

    {% with messages = get_flashed_messages(with_categories=true) %}
      {% if messages %}
        {% for category, message in messages %}
          <div class="alert alert-{{ category }} alert-dismissible fade show" role="alert">
            {{ message }}
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
        {% endfor %}
      {% endif %}
    {% endwith %}

    <h4 class="mt-4">Your Medicines</h4>
    <div class="table-responsive">
        <table class="table table-striped table-hover align-middle">
            <thead class="table-dark">
                <tr>
                    <th>Name</th>
                    <th>Type</th>
                    <th>Dosage</th>
                    <th>Count</th>
                    <th>Before/After Food</th>
                    <th>Expiry Date</th>
                    <th>Status</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                {% for med in medicines %}
                <tr>
                    <td>{{ med.name }}</td>
                    <td>{{ med.type }}</td>
                    <td>{{ med.dosage }} {{ med.dosage_unit }}</td>
                    <td>{{ med.count }}</td>
                    <td>{{ med.before_after_food }}</td>
                    <td>{{ med.expiry_date }}</td>
                    <td>
                        {% if med.status == "Taken" %}
                            <span class="badge bg-success">Taken</span>
                        {% elif med.status == "Pending" %}
                            <span class="badge bg-warning text-dark">Pending</span>
                        {% else %}
                            <span class="badge bg-danger">Not Taken</span>
                        {% endif %}
                    </td>
                    <td>
                        <a href="{{ url_for('delete_medicine', id=med.id) }}" class="btn btn-danger btn-sm btn-animate mb-1">Delete</a>
                        <a href="{{ url_for('mark_medicine', id=med.id, status='Taken') }}" class="btn btn-success btn-sm btn-animate mb-1">Mark Taken</a>
                        <a href="{{ url_for('mark_medicine', id=med.id, status='Pending') }}" class="btn btn-warning btn-sm btn-animate mb-1">Pending</a>
                        <a href="{{ url_for('mark_medicine', id=med.id, status='Not Taken') }}" class="btn btn-danger btn-sm btn-animate mb-1">Not Taken</a>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
